import React from 'react';
import './style.scss';
// COMPONENTS
import CartContainer from '../../features/CartContainer/component';

const CartPage = () => {
    return (
        <main>
            <CartContainer />
        </main>
    );
}

export default CartPage;